<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.box.auth.mapper.SysLogininforMapper">

    <resultMap  id="SysLogininforResult" type="com.box.auth.domain.SysLogininfor">
        <id     property="infoId"        column="info_id"           />
        <result property="userName"      column="user_name"         />
        <result property="status"        column="status"            />
        <result property="ipAddr"        column="ipaddr"            />
        <result property="msg"           column="msg"               />
        <result property="accessTime"    column="access_time"       />
    </resultMap>

    <insert id="insertLogininfor" parameterType="com.box.auth.domain.SysLogininfor">
        insert into sys_logininfor (user_name, status, ipaddr, msg, access_time)
        values (#{userName}, #{status}, #{ipAddr}, #{msg}, sysdate())
    </insert>
</mapper>
